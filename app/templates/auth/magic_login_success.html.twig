{% extends 'base.html.twig' %}

{% block title %}Вы успешно вошли{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        body {
            margin: 0;
            min-height: 100vh;
            background: #f5f7fb;
            font-family: "Inter", system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
            color: #1f2937;
        }

        .magic-login-wrapper {
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            padding: 24px;
        }

        .magic-login-card {
            background: #ffffff;
            border: 1px solid #e5e7eb;
            border-radius: 16px;
            box-shadow: 0 18px 40px rgba(15, 23, 42, 0.08);
            max-width: 520px;
            width: 100%;
            padding: 40px 32px;
            text-align: center;
        }

        .magic-login-card h1 {
            font-size: 32px;
            margin: 0 0 16px;
            color: #111827;
        }

        .magic-login-card p {
            margin: 8px 0;
            line-height: 1.6;
            font-size: 16px;
        }

        .magic-login-card .hint {
            color: #6b7280;
            font-size: 14px;
        }
    </style>
{% endblock %}

{% block body %}
    <div class="magic-login-wrapper">
        <div class="magic-login-card">
            <h1>Вы успешно вошли</h1>
            <p>Ссылка подтверждена. Теперь вы можете вернуться в приложение или закрыть эту вкладку.</p>
            <p class="hint">Если у вас открыто приложение или чат с ботом, оно уже должно обновиться автоматически.</p>
        </div>
    </div>
{% endblock %}
