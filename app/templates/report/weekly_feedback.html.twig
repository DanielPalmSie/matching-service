<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Weekly Feedback Report</title>
    <style>
        body { font-family: 'DejaVu Sans', Arial, sans-serif; margin: 20px; color: #1f2937; }
        h1 { color: #0f172a; margin-bottom: 5px; }
        h2 { color: #111827; margin-top: 30px; margin-bottom: 10px; }
        .muted { color: #6b7280; font-size: 14px; }
        table { width: 100%; border-collapse: collapse; margin-top: 10px; }
        th, td { border: 1px solid #e5e7eb; padding: 10px; text-align: left; vertical-align: top; }
        th { background: #f3f4f6; font-weight: 600; }
        tr:nth-child(even) { background: #f9fafb; }
        .tag { display: inline-block; padding: 2px 6px; background: #e0f2fe; color: #075985; border-radius: 4px; font-size: 12px; }
    </style>
</head>
<body>
    <h1>Weekly Feedback Report</h1>
    <div class="muted">Period: {{ range.start }} to {{ range.end }} &mdash; Total comments: {{ totalComments }}</div>

    <h2>Feedback Clusters</h2>
    <table>
        <thead>
        <tr>
            <th>#</th>
            <th>Topic</th>
            <th>%</th>
            <th>Description</th>
            <th>Examples</th>
        </tr>
        </thead>
        <tbody>
        {% if clusters is empty %}
            <tr><td colspan="5">No clusters available.</td></tr>
        {% else %}
            {% for cluster in clusters %}
                <tr>
                    <td>{{ loop.index }}</td>
                    <td><span class="tag">{{ cluster.label }}</span></td>
                    <td>{{ cluster.percentage|default(0) }}%</td>
                    <td>{{ cluster.description }}</td>
                    <td>
                        <ul>
                            {% for example in cluster.examples|default([]) %}
                                <li>{{ example }}</li>
                            {% endfor %}
                        </ul>
                    </td>
                </tr>
            {% endfor %}
        {% endif %}
        </tbody>
    </table>

    <h2>Aggregated Statistics</h2>
    <h3>Reason Codes</h3>
    <table>
        <thead>
        <tr><th>Reason</th><th>Count</th></tr>
        </thead>
        <tbody>
        {% if reasonCodes is empty %}
            <tr><td colspan="2">No data available.</td></tr>
        {% else %}
            {% for item in reasonCodes %}
                <tr><td>{{ item.label|default('N/A') }}</td><td>{{ item.total }}</td></tr>
            {% endfor %}
        {% endif %}
        </tbody>
    </table>

    <h3>Main Issues</h3>
    <table>
        <thead>
        <tr><th>Issue</th><th>Count</th></tr>
        </thead>
        <tbody>
        {% if mainIssues is empty %}
            <tr><td colspan="2">No data available or column missing.</td></tr>
        {% else %}
            {% for item in mainIssues %}
                <tr><td>{{ item.label|default('N/A') }}</td><td>{{ item.total }}</td></tr>
            {% endfor %}
        {% endif %}
        </tbody>
    </table>

    <h3>Average Relevance Score</h3>
    <p>{{ averageRelevance|number_format(2, '.', ',') }}</p>
</body>
</html>
